name: Build static AI news pages

on:
  push:
    paths:
      - 'news.json'          # déclenché uniquement par le push Google Sheet
  workflow_dispatch:         # permet aussi de lancer manuellement

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build article pages
        run: node scripts/build-ai-news-pages.mjs

      - name: Commit and push generated pages
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add articles/
          git add news-latest.json
          git diff --cached --quiet && echo "Nothing to commit" && exit 0
          git commit -m "chore: rebuild static pages [skip ci]"
          git push
